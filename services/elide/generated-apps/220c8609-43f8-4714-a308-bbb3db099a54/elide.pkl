amends "elide:project.pkl"

name = "try that again but instead of returning markdown, "
description = "Generated Elide polyglot application"
version = "1.0.0"

entrypoint = "App.tsx"

dependencies {
  npm {
    packages {
      "react@18"
      "react-dom@18"
    }
  }
  
}